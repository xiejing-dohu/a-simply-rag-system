# 智能 RAG 系统项目说明

## 1. 项目定位

本项目是一个前后端分离、多用户隔离的智能问答与知识库系统。它支持用户注册、
登录保护、SSE 流式回答、模型自动发现、个人 Token 用量与管理员配额、文档解析
和切片、Milvus 向量化入库，以及语义、Dense、混合三种 RAG 检索方式。

当前版本已将用户、聊天、Token 统计、知识库元信息和文档任务持久化到 MySQL；
Refresh Token 与登录锁定使用 Redis，切片和 Dense/Sparse 向量存储在 Milvus。

详细文档：

- `Frontend/README.md`：前端结构、全部接口字段、错误码和 SSE 协议；
- `Backend/README.md`：后端分层、存储边界、文档入库、RAG 和运行维护。

## 2. 技术栈

| 层级 | 技术 | 职责 |
| --- | --- | --- |
| 前端 | Vue 3 + TypeScript | 页面和类型安全 |
| 构建 | Vite | 开发服务器与生产构建 |
| UI/状态 | Element Plus + Pinia | 组件、用户/聊天/知识库状态 |
| 网络 | Axios + Fetch ReadableStream | REST 和 POST SSE |
| 后端 | FastAPI + Uvicorn | API、认证依赖、流式响应 |
| 项目管理 | uv | Python 依赖、虚拟环境和锁文件 |
| 上游模型 | OpenAI-compatible API + HTTPX | 模型发现、聊天和 Embedding |
| 文档处理 | python-docx、pandas、pypdf | DOCX、Excel、PDF 和文本提取 |
| 切片 | tiktoken | 按 Token 数和 overlap 切片 |
| 关系数据 | MySQL 8 + SQLAlchemy async + aiomysql | 知识库和文档元信息 |
| 缓存/安全 | Redis 7 | Refresh Token、登录锁定、文档任务队列 |
| 向量数据 | Milvus 2.5 + PyMilvus | Dense、BM25 Sparse、切片和混合检索 |
| Milvus 依赖 | etcd + MinIO | 内部元数据和对象数据 |
| 容器 | Docker Compose | 仅管理数据库相关服务 |

## 3. 总体架构

```text
浏览器
  │
  ├─ REST JSON ─────────────┐
  └─ POST SSE 流式聊天 ─────┤
                            ▼
                     FastAPI 后端
                 ┌──────────┼───────────┐
                 │          │           │
             模型服务      MySQL      Milvus
         /models/chat/embed  │           │
                            │       etcd + MinIO
                            │
                  知识库和文档元信息
```

存储职责：

| 存储 | 当前内容 |
| --- | --- |
| MySQL | 用户、会话、消息、Token 用量、知识库、文档、异步任务 |
| Redis | Refresh Token、失败锁定 TTL、持久化任务队列 |
| Milvus | 文档切片、来源字段、Dense 向量、BM25 Sparse 向量 |
| etcd / MinIO | Milvus 自身的元数据和对象数据 |

## 4. 项目结构

```text
智能rag系统/
├── 项目说明文件.md
├── start.ps1 / start.sh            # Windows / Bash 一键启动
├── Frontend/
│   ├── README.md                    # 前端及详细接口说明
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── api/                     # REST/SSE 请求封装
│       ├── components/              # 布局、消息、模型和 Token 组件
│       ├── router/                  # 页面路由与守卫
│       ├── stores/                  # Pinia 状态
│       ├── types/                   # TypeScript 接口类型
│       └── views/                   # 登录、聊天、知识库、管理页面
└── Backend/
    ├── README.md                    # 后端整体架构说明
    ├── .env                         # 数据库与模型服务配置
    ├── pyproject.toml
    ├── uv.lock
    ├── Dockerfile
    ├── docker-compose.yml
    ├── alembic.ini
    ├── migrations/                  # Alembic 版本化迁移
    ├── tests/                       # 端到端与 Worker 集成测试
    └── app/
        ├── main.py                  # FastAPI 应用工厂和 Router 注册
        ├── api/                     # 按认证/聊天/知识库/模型拆分的路由
        ├── schemas/                 # 按业务领域拆分的 API 契约
        ├── services/                # 模型发现和流式模型调用
        ├── state_repository.py      # 用户、会话、消息和 Token 仓储
        ├── knowledge_repository.py  # 知识库 MySQL 仓储
        ├── document_tasks.py        # MySQL 租约和异步入库 Worker
        ├── vector_outbox.py         # Milvus Saga/Outbox
        ├── worker.py                # 独立后台进程入口
        ├── knowledge_runtime.py     # 文档、Embedding、Milvus
        ├── rag/retriever.py         # 三种检索及 Prompt
        ├── db/                      # 数据库连接
        ├── models/                  # ORM 模型
        └── core/                    # 配置、安全和共享异常
```

## 5. 核心业务流程

### 5.1 登录与权限

- 普通用户可注册，身份固定为 `employee`。
- 密码使用 PBKDF2-SHA256 哈希；Access JWT 有效期可配置。
- Refresh Token 和连续 3 次密码错误后的 5 分钟锁定由 Redis TTL 管理。
- 默认系统管理员不能降级或停用。
- 只有系统管理员能修改身份；后续被提升的管理员没有身份修改权限。
- 后端对管理操作做最终校验，前端路由守卫只用于改善交互。

### 5.2 用户聊天隔离

会话和消息均记录服务端 `user_id`。会话列表、消息列表、发送、删除、切换模型和
RAG 设置都会验证资源归属。接口响应不暴露消息的内部用户 ID。

### 5.3 模型发现和流式回复

```text
Backend/.env
  → OPENAI_API_BASE + API Key
  → 后端探测上游 /models
  → 过滤非聊天模型并缓存 5 分钟
  → 前端获取列表并自动选择模型
```

发送消息时后端调用上游 `/chat/completions`，将文本增量转为 SSE。启用 RAG 时
先发送召回上下文事件，再发送回答文本，最后发送 `[DONE]`。Token usage 从上游
最终流数据中读取并按用户累计。

### 5.4 知识库入库

```text
创建知识库并选择向量维度
  → 创建 Milvus Collection
  → MySQL 保存知识库/Collection 映射
  → 上传后立即获得 task_id
  → Redis 唤醒独立 Worker，MySQL 行锁领取可靠任务
  → Worker 异步提取全文（Excel 先拼接全部工作表）
  → tiktoken 切片
  → .env 的 EMBEDDING_MODEL 向量化
  → 以 task_id + chunk_index 生成稳定切片 ID，Milvus upsert 切片和向量
  → MySQL 以唯一 ingestion_id 幂等保存文档处理统计
```

支持 DOCX、XLSX、XLS、PDF、TXT、Markdown、CSV，单文件最大 30 MB。前端从
后端获取实际 Embedding 模型、支持的扩展名和可选维度，不硬编码这些配置。
Worker 被取消时任务会恢复为 queued 并保留临时文件；旧版 auto-ID Collection
仍可检索，但不再接受新上传，需新建知识库并迁移文档。

### 5.5 RAG 检索

聊天发送前可开启 RAG，选择知识库、检索方式和最大召回 Token：

| 模式 | 算法 |
| --- | --- |
| 语义检索 `semantic` | Dense 候选 + MMR 去重重排 |
| Dense 检索 `dense` | Milvus COSINE 相似度排序 |
| 混合检索 `hybrid` | Milvus Dense + BM25 Sparse，通过 RRF 融合 |

旧版 Collection 没有 Sparse 字段时自动回退到应用层 BM25。可选 DashScope
`qwen3-rerank` 会对初筛候选再次打分；未配置时保持召回排序。

召回内容按 128–16000 的 Token 预算装入，最后一个切片可被截断。后端把资料注入
system 消息，并在 SSE 的 `rag` 事件和历史助手消息中保留来源。

### 5.6 Token 用量

每位用户拥有：

- 累计输入、输出和总 Token；
- 当前 5 小时窗口用量及上限；
- 当前 7 天窗口用量及上限。

两个上限可设置具体正整数或无限。达到任一上限时聊天返回 429。管理员可独立重置
5 小时、周窗口或两者；用户可在客户端查看自己的模式、用量和下次重置时间。

## 6. 配置

`Backend/.env` 至少需要数据库、Milvus 和模型服务配置：

```env
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=请设置密码
MYSQL_DATABASE=rag_system

MILVUS_HOST=127.0.0.1
MILVUS_PORT=19530

OPENAI_API_KEY=请设置密钥
OPENAI_API_BASE=https://example.com/compatible-mode/v1
DEFAULT_MODEL=qwen-plus-latest
EMBEDDING_MODEL=text-embedding-v4
```

不要把真实 API Key 写入前端环境变量或提交到版本控制。

## 7. 本地运行

本项目不把前后端放入 Docker；Docker Compose 仅运行 MySQL 和 Milvus 依赖。

### 7.1 启动数据服务

```powershell
cd D:\智能rag系统\Backend
docker compose up -d mysql redis etcd minio milvus
docker compose ps
```

### 7.2 启动后端

```powershell
cd D:\智能rag系统\Backend
uv sync
uv run alembic upgrade head
uv run uvicorn app.main:app --reload --port 8000
```

- API：`http://localhost:8000`
- 健康检查：`http://localhost:8000/health`
- Swagger：`http://localhost:8000/docs`

### 7.3 启动独立 Worker

另开一个 PowerShell：

```powershell
cd D:\智能rag系统\Backend
uv run python -m app.worker
```

### 7.4 启动前端

另开一个 PowerShell：

```powershell
cd D:\智能rag系统\Frontend
npm install
npm run dev
```

前端：`http://localhost:5173`

默认本地管理员为 `admin / admin123`，仅供开发使用。

## 8. 服务端口

| 服务 | 地址 |
| --- | --- |
| 前端 | `http://localhost:5173` |
| FastAPI | `http://localhost:8000` |
| MySQL | `127.0.0.1:3306` |
| Milvus | `127.0.0.1:19530` |
| Milvus 健康接口 | `http://localhost:9091/healthz` |
| MinIO API | `http://localhost:9000` |
| MinIO 控制台 | `http://localhost:9001` |

## 9. 当前限制

- 默认 `admin / admin123` 仅适用于本地开发，部署前必须修改。
- 数据库使用 Alembic 迁移，API 启动时只校验迁移版本，不执行 `create_all`。
- 知识库创建/删除使用 MySQL 事务 Outbox、幂等 Milvus Worker、指数退避重试和
  定时对账；文档入库通过稳定切片 ID、Milvus upsert 和唯一 ingestion_id 幂等重试。
- 后台 Worker 已与 FastAPI 分离，文档任务使用 MySQL 租约，可运行多个实例。
- 用户名注册、登录和失败锁定统一进行首尾去空格及小写化。
- Embedding 服务额度不可用时，文档向量化和 RAG 查询会失败；需要检查上游账户
  额度和 `.env` 配置。
- 用户/聊天/配额持久化、密码哈希、JWT、异步入库和端到端测试均已落地。
